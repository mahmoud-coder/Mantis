<div class="product-container">
    <div class="product">
    <img src="{{$thumbnail}}" class="product-thumbnail">
    <a href="{{route('product',['slug' => $slug])}}" class="product-btn">{{$title}}</a>
    </div>
</div>